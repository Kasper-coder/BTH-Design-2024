{% include 'incl/header.twig' %}

<body>
    {% include 'incl/nav.twig' %}

    <div class="main" role="main">
        <div class="landingpage">
            {{ content }}

            {% for rep in meta.reps %}
                <a href="{{ base_url }}/analysis/{{ rep.class }}">
                    <div class="box {{ rep.class }}"
                        <h1>{{ rep.name }}</h1>
                    </div>
                </a>
            {% endfor %}
        </div>
    </div>

    {% include 'incl/footer.twig' %}
</body>

</html>