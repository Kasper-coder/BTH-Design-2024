<div class="footer">
    <div class="footer-content">
        <h3>Contact me</h3>
        <ul class="contact">
            <li><a href="mailto:holgerssonkasper@gmail.com">holgerssonkasper@gmail.com</a></li>
        </ul>
        <ul class="socials"> <!-- Use the 'socials' class for consistent styling -->
            {% for social in pages["_meta"].meta.social %}
                <li>
                    <a href="{{ social.url }}" title="{{ social.title }}" role="button">
                        <i class="fa fa-{{ social.icon }}" aria-hidden="true"></i>
                        <span class="sr-only">{{ social.title }}</span>
                    </a>
                </li>
            {% endfor %}
        </ul>
    </div>
</div>